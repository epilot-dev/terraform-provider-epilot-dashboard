terraform {
  required_providers {
    epilot-dashboard = {
      source  = "epilot-dev/epilot-dashboard"
      version = "0.15.1"
    }
  }
}

provider "epilot-dashboard" {
  # Configuration options
}